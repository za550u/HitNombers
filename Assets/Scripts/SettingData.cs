using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SettingData : MonoBehaviour {

    static int  g_selectStage;      // ステージ選択
    static bool g_chooceFlag;       // チョイス有無
    static int  g_cardSet;          // カードセット
    static bool g_territoryFlag;    // 陣地
    static bool g_timeLimitFlag;    // 制限時間有無

    static string g_iword;          // 合言葉    

	// Use this for initialization
	void Start () {
		// ファイルを読み込む
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
